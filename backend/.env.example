NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://user:password@localhost:5432/wedding_app?schema=public
CLIENT_ORIGIN=http://localhost:3000

# JWT (not used yet but ready)
JWT_SECRET=change_me
JWT_EXPIRES_IN=1d

EMAIL_VERIFY_JWT_SECRET=change_me
EMAIL_VERIFY_JWT_EXPIRES_IN=1d

# Rate limit
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

MAIL_HOST=smtp.mailtrap.io        # or smtp.gmail.com, email-smtp.us-east-1.amazonaws.com, etc.
MAIL_PORT=587                     # 465 for SSL, 587 for STARTTLS
MAIL_USER=your_smtp_user
MAIL_PASS=your_smtp_pass
MAIL_FROM="Your App <no-reply@yourapp.com>"
